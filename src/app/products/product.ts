export interface IProduct { // export ensures other parts of application can use this interface
    productId: number;
    productName: string;
    productCode: string;
    releaseDate: string;
    price: number;
    description: string;
    starRating: number;
    imageUrl: string;    
}